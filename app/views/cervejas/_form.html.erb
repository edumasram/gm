<%= form_for(@cerveja) do |f| %>
  <% if @cerveja.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cerveja.errors.count, "error") %> prohibited this cerveja from being saved:</h2>

      <ul>
      <% @cerveja.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :descricao %><br />
    <%= f.text_area :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
